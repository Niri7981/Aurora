{
  "default_disclosure_policy": {
    "mcp": "read-only and minimum-necessary disclosure for authorized agents",
    "raw_imports": "never disclose raw imported sources by default",
    "durable_memory": "return only current, approved, task-relevant records"
  },
  "blocked_without_confirmation": [
    "secrets",
    "credentials",
    "financial records",
    "private journal entries",
    "local-only lines"
  ],
  "redaction_markers": [
    "private:",
    "local-only:"
  ]
}
